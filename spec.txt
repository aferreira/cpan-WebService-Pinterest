

sub _auth_code {
        # GET /oauth
        #    response_type: code
        #    client_id: <app_id>
        #    state:     <state>
        #    scope:     <permission scopes>, eg 'read_public,write_public'
        #    redirect_uri
}

 endpoint => '/oauth',
 parameters => (
      response_type => 'code',
      client_id => qr/^[0-9]$/, 'Int',
      state => 'Str',
      scope => list of 'read_public' 'write_public'
      redirect_uri => https URI
 );
